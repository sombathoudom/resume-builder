{
  "name": "cv-builder",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev"
  },
  "dependencies": {
    "zod": "^4.1.9"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
